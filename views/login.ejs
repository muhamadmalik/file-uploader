<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File Uploader - Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-[#1a1a1a] text-white min-h-screen">
    <header
      class="flex justify-between items-center p-4 border-b border-gray-800"
    >
      <h1 class="text-xl font-semibold">File Uploader</h1>
      <div class="space-x-2">
        <a
          href="/auth/login"
          class="px-4 py-1.5 rounded bg-transparent border border-gray-700 hover:bg-gray-800 transition"
          >Login</a
        >
        <a
          href="/auth/signup"
          class="px-4 py-1.5 rounded bg-transparent border border-gray-700 hover:bg-gray-800 transition"
          >Sign Up</a
        >
      </div>
    </header>

    <main class="flex justify-center items-center min-h-[80vh]">
      <div class="w-full max-w-md p-6">
        <h2 class="text-2xl font-semibold mb-6">Log in</h2>

        <% if (locals.error) { %>
        <div
          class="bg-red-500/10 border border-red-500/20 text-red-500 rounded p-3 mb-4"
        >
          <%= error %>
        </div>
        <% } %>

        <form action="/auth/login" method="POST" class="space-y-4">
          <div>
            <label for="username" class="block mb-2">Username</label>
            <input
              type="text"
              id="username"
              name="username"
              required
              class="w-full p-2 rounded bg-[#2a2a2a] border border-gray-700 focus:outline-none focus:border-gray-500"
            />
          </div>

          <div>
            <label for="password" class="block mb-2">Password</label>
            <input
              type="password"
              id="password"
              name="password"
              required
              class="w-full p-2 rounded bg-[#2a2a2a] border border-gray-700 focus:outline-none focus:border-gray-500"
            />
          </div>

          <button
            type="submit"
            class="w-24 px-4 py-2 bg-[#2a2a2a] border border-gray-700 rounded hover:bg-gray-800 transition"
          >
            Log In
          </button>
        </form>

        <p class="mt-4 text-gray-400">
          Don't have an account yet?
          <a href="/auth/signup" class="text-blue-400 hover:underline">Sign up</a>
        </p>
      </div>
    </main>
  </body>
</html>
